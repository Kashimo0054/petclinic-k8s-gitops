apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: petclinic

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: petclinic-ebs

  resources:
    requests:
      storage: 10Gi
